<form method="post" action="{{ path('admin_reponserec_delete', {'reponseId': reponserec.reponseId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponserec.reponseId) }}">
    <button class="btn">Delete</button>
</form>
