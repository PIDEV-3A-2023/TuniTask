{% extends 'base2.html.twig' %}

{% block title %}Reponse Rec{% endblock %}

{% block body %}
 <div class="content-body">
    <h1>Reponse Rec</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nomagent</th>
                <td>{{ reponserec.nomagent }}</td>
            </tr>
            <tr>
                <th>Prenomagent</th>
                <td>{{ reponserec.prenomagent }}</td>
            </tr>
            <tr>
                <th>Resolution</th>
                <td>{{ reponserec.resolution }}</td>
            </tr>
            <tr>
                <th>Reclamation</th>
                <td>{{ reponserec.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_reponserec_index') }}">back to list</a>

    {{ include('reponserecAdmin/_delete_form.html.twig') }}
    </div>
{% endblock %}
